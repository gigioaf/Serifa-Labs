---
// Preloader curtain. The "S" (.pre-s) fades in and the curtain lifts —
// animated by effects-gsap.ts. With reduced motion or if GSAP never loads,
// effects-base.ts removes it immediately (the page is never stuck).
---
<div id="preloader" aria-hidden="true"><span class="pre-s"></span></div>
